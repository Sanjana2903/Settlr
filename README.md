# Settlr

India-first shared-expense + settlement app. Split. Pay. Done.

## Structure

- `app/` — React Native (Expo) mobile app
- `supabase/` — database schema, RLS policies, and edge functions (added in Phase 1)

## Development

See `app/README.md` for running the mobile app locally.
